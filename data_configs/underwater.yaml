# Images and labels direcotry should be relative to train.py
TRAIN_DIR_IMAGES: data/underwater_voc/train
TRAIN_DIR_LABELS: data/underwater_voc/train
# VALID_DIR should be relative to train.py
VALID_DIR_IMAGES: data/underwater_voc/valid
VALID_DIR_LABELS: data/underwater_voc/valid

# Class names.
CLASSES: [
    '__background__',
    'Sea Pen',
    'Starfish'
]

# Number of classes (object classes + 1 for background class in Faster RCNN).
NC: 3

# Whether to save the predictions of the validation set while training.
SAVE_VALID_PREDICTION_IMAGES: True